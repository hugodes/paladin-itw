<!DOCTYPE html>
<html>
<head>
  <title>New Booking Notification</title>
  <style>
    :root {
      --background: #ffffff;
      --foreground: #171717;
      --primary-color: #0070f3;
      --primary-color-hover: #005bb5;
    }
    body {
      color: var(--foreground);
      background: var(--background);
      font-family: Arial, Helvetica, sans-serif;
      padding: 2rem;
    }
    .container {
      background-color: var(--background);
      padding: 2rem;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      border-radius: 8px;
    }
    .title {
      font-size: 2.5rem;
      margin-bottom: 2rem;
      color: var(--foreground);
    }
    .content {
      font-size: 1.2rem;
      margin-bottom: 2rem;
    }
    .link {
      color: var(--primary-color);
      text-decoration: none;
    }
    .link:hover {
      color: var(--primary-color-hover);
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="title">Notification de Nouvelle Réservation 📆</h1>
    <p class="content">Bonjour <%= name %>,</p>
    <ul class="content">
      <li>Date et heure : <%= new Date(date).toLocaleDateString('fr-FR', { day: '2-digit', month: '2-digit', year: 'numeric' }) %> de <%= startTime %> à <%= endTime %></li>
      <li>Raison : <%= reason %></li>
      <li>Commentaire : <%= comment %></li>
    </ul>
    <p class="content">Rejoignez la réunion : <a class="link" href="<%= link %>"><%= link %></a></p>
    <p class="content">Merci,<br>L'équipe Paladin 🛡️</p>
  </div>
</body>
</html>